name: internet_popup
description: A package for popUp message when Internet connection is off. Write one line to initialize a popUp when internet connection is turned off or connected but no internet.

version: 1.0.8+3
homepage: https://github.com/mehedidew/internet_popup

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  data_connection_checker_tv: ^0.3.5-nullsafety
  connectivity_plus: ^2.3.0


dev_dependencies:
#  flutter_test:
#    sdk: flutter
  test: ^1.19.2
  flutter_lints: ^2.0.1


